Sending data from top to bottom is easy through props, 
but sending data through bottom to top is also easy -> using functions props

- We pass a function to an children as props
- We use the function in children and pass an argument, 
- that data is passed to parent, and then used.
